/*
Combine Strings and Numbers, Write a function that merges a piece of text with a number.
*/
const joinUs = (string:string, number:number) => {
    return string + number;
}
console.log("My ATM Pass is", 1234);
