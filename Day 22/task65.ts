/**
 * Determine the Remainder: Make a function that gets two number and shows the leftover from dividing them using % sign. 
 */
const getRemainder = (value1:number, value2:number):number =>{
    return value1 % value2;
}
console.log(getRemainder(5,2))